[pytest]
pythonpath = .
testpaths = tests
markers =
    contract: validates public prediction Decision Object contract
    integration: end-to-end integration tests
    llm_integration: tests real LLM/API integration
